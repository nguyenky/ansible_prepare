- name: reset ownership & permissions of a storage
  file:
    path: "{{ deploy_to }}/releases"
    state: directory
    owner: "{{ deploy_user }}"
    group: "{{ deploy_group }}"
    recurse: yes
    mode: 0755
  become: yes
  become_method: sudo
  become_user: root