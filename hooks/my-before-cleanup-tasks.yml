- name: reset ownership & permissions of a storage
  file:
    path: "{{ release_path }}/storage"
    state: directory
    recurse: true
    mode: 0775
    follow: yes
  become: yes
  become_method: sudo
  become_user: root
